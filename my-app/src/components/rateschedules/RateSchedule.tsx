import * as React from "react";

interface IRateScheduleProps {}

const RateSchedule: React.FunctionComponent<IRateScheduleProps> = (props) => {
  return (
    <>
      <h2>RateSchedule Component</h2>
    </>
  );
};

export default RateSchedule;
