import * as React from "react";
import "./IconStyle.css";
interface IRightIconProps {}

const RightIcon: React.FunctionComponent<IRightIconProps> = (props) => {
  return (
    <>
      <div className="rightIcon"></div>
    </>
  );
};

export default RightIcon;
